** gpu monitoring
watch -n 1 nvidia-smi

** show ip
netstat -ntlp

** kill port
kill -9 [pid]

﻿Linux(ubuntu) command

alias ls='ls -F --color=auto --show-control-chars'

** find text
grep -rnw [text]

** change hostname *
sudo hostname [new name]

** change hostname (permanently) **
sudo vi /etc/hostname
sudo vi /etc/hosts

***git install
sudo apt-get install git

*** nimf Hangul inputter
$ sudo apt-add-repository ppa:hodong/nimf
$ sudo apt update
$ sudo apt install nimf
$ im-config -n nimf

***(Dasom 한글입력기 install) -> Nimf로 이름변경, old version, do not install.
sudo add-apt-repository ppa:dasom/ppa
sudo apt-get update && sudo apt-get install dasom dasom-jeongeum dasom-gtk dasom-qt
im-config
-->log out and log in again


*** 압축풀때 한글 파일이름 깨짐 문제.
터미널에서, unzip -O cp949 [파일이름]
(대문자 -O, 윈도우에서 사용하는 텍스트 인코딩이 cp949 라고 명시해줌)

****Ubuntu Text Editor(gedit) 한글 인코딩 추가
 -윈도우 인코딩 : EucKR or UHC(Unified Hangul code), EucKR : 2350자 표현가능.
 -Ubuntu Encoding : UTF-8 / UTF-16/ISO 8859-15
sudo apt-get install dconf-tools
dconf-editor
org>gnome>gedit>proference>encode
['UHC','UFT-8','CURRENT','ISO-8859-15','UTF-16]
 
--hibernation
sudo systemctl hibernate

***Alternative to Notpad++ for Linux.
1. Notepadqq ( is exact replica of Notepad++)
sudo add-apt-repository ppa:notepadqq-team/notepadqq
sudo apt-get update
sudo apt-get install notepadqq
2.SciTE(cross platform, open source)
-It had been in the Linux world for quite sometime and has a fan follwing of tis own.
-sudo apt-get install scite
3.Geany(almost IDE)
-sudo apt-get install geany scite
4.Sublime Text
-non-free, non-open source
-sudo add-apt-repository ppa:webupd8team/sublime-text-2
-sudo apt-get update
-sudo apt-get install sublime-text
5.Lime text(is actually related to Sublime text)
-free and open source, clone of SublimeText)
6.Kate
-default editor of KDE desktop.
-sudo apt-get install kate
7.Atom
-the latest sensation in programming world.
-cross platform, open source.



Sudo dpkg -i [pkg name]  : deb 패키지 파일 설치
apt list --installed : 설치된 패키지 보기
compgen -g  :그룹보기
compgen -u  :사용자보기
groups jw      : 사용자가 속한 그룹 보기

--Permission 바꾸기
sudo chown -R username:group directory
sudo chown username:group directory
dpkg --get-selections | grep -v deinstall
(The -v tag "inverts" grep to return non-matching lines)


--apache2 서버루트 변경
  etc/apache2/apache2.conf
  <Directory /home/jw/www>  -->이부분 변경
  	Options Indexes FollowSymLinks
  	AllowOverride None
  	Require all granted
  </Directory>

  etc/apache2/sites-enabled/000-default.conf
  DocumentRoot /home/jw/www  --> 이부분 변경

--php.ini (etc.php/5.6/apache2)
  opcache enabled =0
  display_errors = On

ls -al |grep vim : 특정어구 포함 리스트 출력
